# Pinned exactly so the VM does not pick up a breaking release on its own.
# Yahoo/yfinance was removed — Angel One SmartAPI only.
pandas==2.3.3
numpy==2.2.6
requests==2.34.2
pytz==2026.3.post1
pyotp==2.10.0
