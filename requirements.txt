# Loose enough for Ubuntu 20.04 / Python 3.8 (Always Free VM) and newer.
# Do not pin pandas 2.2+ / numpy 2.x — those need Python 3.9+.
pandas>=2.0.3,<2.1
numpy>=1.24.4,<1.25
requests>=2.31.0,<3
pytz
pyotp>=2.9.0,<3
